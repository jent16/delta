# Copy to .env and fill in. Never commit the real .env (already gitignored).

# Free developer account at https://developer.adzuna.com/
ADZUNA_APP_ID=
ADZUNA_APP_KEY=

# API key (separate from a Claude Code session) from https://console.anthropic.com/
ANTHROPIC_API_KEY=
